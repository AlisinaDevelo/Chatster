# Optional: override WebSocket URL (production or custom dev)
# REACT_APP_WS_URL=wss://api.example.com/ws

# Dev only: backend port when using default ws://127.0.0.1:<port>/ws
# REACT_APP_WS_PORT=8080
